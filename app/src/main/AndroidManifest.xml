<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bookstore"
        tools:targetApi="31">
        <meta-data
            android:name="cloudinary_url"
            android:value="${cloudinaryUrl}" />

        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Bookstore">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.note.NoteActivity" />
        <activity android:name=".ui.payment.PaymentMethodActivity" />
        <activity android:name=".ui.note.NoteEditorActivity" />
        <activity android:name=".ui.home.HomeActivity" />
        <activity android:name=".ui.book.BookDetailActivity" />
        <activity android:name=".ui.book.AddBookActivity" />
        <activity android:name=".ui.book.EditBookActivity" />
        <activity android:name=".ui.book.BookManagementActivity" />
        <activity android:name=".ui.user.NotificationActivity" />
        <activity android:name=".ui.user.OrderManagementActivity" />
        <activity android:name=".ui.user.UserManagementActivity" />
        <activity android:name=".ui.cart.CartActivity" />
        <activity android:name=".ui.auth.RegisterActivity" />
        <activity android:name=".ui.chat.ChatActivity" />

    </application>

</manifest>